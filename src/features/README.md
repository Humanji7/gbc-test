# Features

This directory groups domain-specific code as the project grows.

- `orders/` owns RetailCRM import and sync transforms.
- `alerts/` owns high-value order notification logic.
- `dashboard/` owns data shaping for metrics views.
